apiVersion: v1
kind: Service

metadata:
  name: devops-eks-service

spec:
  type: LoadBalancer

  selector:
    app: devops-eks-app

  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
